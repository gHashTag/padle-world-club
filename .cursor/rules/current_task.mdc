---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Великая Цель и Текущий Этап (Telegram Bot Starter Kit)

**Великая цель:**
Создать универсальный Telegram Bot Starter Kit на TypeScript, включающий общие паттерны (WizardScene, DI, StorageAdapter), инфраструктуру тестирования (unit, integration, e2e), утилиты, шаблоны и документацию для быстрого старта любого Telegram-бота.

---

# 🕉️ Общие правила

- **Обязательное общение на русском языке** (ответы, логи, документация)
- **Чистота типов:** после каждого изменения кода — проверка типов (`bun run typecheck`)
- **TDD-цикл:** сначала тест, потом код, потом рефакторинг, всегда через скрипт `scripts/tdd-cycle.sh`
- **Git-коммит:** после каждого логического шага — отдельный коммит с осмысленным сообщением
- **Обновление статуса:** после каждого шага — обновление этого файла (current_task.mdc) с новым чекпоинтом и активной задачей
- **Минимизация специфики:** всё, что не является универсальным паттерном, должно быть удалено

---

# 🎯 Текущий активный этап

**Рефакторинг структуры проекта под стартер-кит:**
- Удаление специфичных модулей (Instagram, конкуренты, хештеги, аналитика и т.д.)
- Сохранение и генерализация универсальных паттернов (WizardScene, DI, StorageAdapter, тестовые утилиты)
- Формирование минимальной структуры стартер-кита
- Добавление README и базовой документации

---

# 🛣️ Чекпоинты

1.  ✅ **Провести рефакторинг структуры (удалить всё специфичное, оставить только универсальное)**
    -   ✅ Удалить специфичные сцены: `analytics-scene.ts`, `competitor-scene.ts`, `hashtag-scene.ts`, `project-scene.ts`, `reels-collection-scene.ts`, `scraping-scene.ts` и их wizard-версии.
    -   ✅ Удалить специфичные сервисы: `chatbot-service.ts`, `embeddings-service.ts`, `marketing-analytics-service.ts`, `reels-collection-service.ts`, `transcript-service.ts`, `mock-apify-service.ts`, `mock-scraper-service.ts`.
    -   ⚠️ Удалить/очистить специфичные схемы: `src/db/schema.ts`, `src/schemas/index.ts` (Требуется ручное редактирование).
    -   ✅ Удалить специфичные адаптеры или сильно их упростить. (Заменены на StorageAdapter и MemoryAdapter)
    -   ✅ Удалить большинство тестов, тестирующих специфичную логику.
    -   ✅ Удалить специфичные скрипты.
    -   ✏️ Сохранить: `src/templates/wizard-scene-template.ts`, DI, `StorageAdapter` (интерфейс + MemoryAdapter), универсальные утилиты (`logger`, `validation`, `button-handler`, `keyboard`), инфраструктуру тестирования, универсальные скрипты (`tdd-cycle.sh`, `generate-wizard-scene.ts`, `quality-check.sh` и т.п.), конфигурационные файлы, `src/bot.ts` (очищенный), документацию по паттернам (`WIZARD_SCENE_PATTERNS.md`).
2.  Сделать git commit после каждого логического шага.
3.  После каждого шага обновлять current_task.mdc с новым статусом и следующим активным шагом.

---

➡️ **Следующий активный шаг:**
Проверить и обновить содержимое сохраненных скриптов (`build-and-test.sh`, `README.md` в `scripts/`, `test-utils.sh` и т.д.), чтобы они соответствовали целям стартер-кита. Удалить или закомментировать специфичные для старого проекта части.
