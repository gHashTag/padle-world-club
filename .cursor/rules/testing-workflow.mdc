---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Ритуал Тестирования и Отладки Потока Агентов

**Принцип:** Стабильность и предсказуемость тестового окружения – основа для уверенного движения вперед. Этот ритуал обеспечивает надежный способ запуска тестов и получения результатов без потери контекста.

**Проблема:**
Запуск тестов должен быть надежным и предоставлять результаты непосредственно AI ассистенту без потери контекста из-за фоновых процессов или прерываний сессии.

**Решение (Согласованный Ритуал):**
Использование прямого запуска тестов с помощью `bun test` в `run_terminal_cmd` с `is_background=False`. Bun Test Runner выполняет тесты синхронно и выводит результаты в стандартный вывод.

**🧘‍♂️ Ритуал (Шаги):**

1.  **Подготовка Пространства (Гуру):**
    *   Убедись, что необходимые фоновые сервисы (`bun run build:watch`, `bun run dev:serve`, `bun run dev:start`) запущены вручную в отдельных терминалах (см. [`README.mdc`](mdc:README.mdc)), если тесты требуют работающего приложения (например, интеграционные тесты API).

2.  **Запуск Тестов (AI Ассистент):**
    *   Выполни команду: `bun test`
    *   Используй `run_terminal_cmd` с `is_background=False`.
    *   **Немедленно проанализируй вывод:**
        *   **🔴 Если команда завершилась с ошибкой (exit code != 0):**
            *   Прочитай и пойми ошибку из вывода команды (это могут быть ошибки компиляции TypeScript, ошибки выполнения тестов или ошибки конфигурации Bun).
            *   Исправь ошибку.
            *   **СТОП.** Вернись к началу Шага 2.
        *   **✅ Если команда выполнена успешно:**
            *   Проанализируй отчет Bun Test Runner в выводе.
            *   Определи результат: ✅ PASS (все тесты пройдены) или 🔴 FAIL (есть упавшие тесты).
            *   Проанализируй упавшие тесты (если есть).
            *   Переходи к следующему логическому шагу (исправление ошибок тестов или следующий этап TDD).

**Цель:** Обеспечить надежный, неблокирующий (для AI) способ запуска тестов и получения их результатов, сохраняя контекст между шагами.



*Ом Шанти. Терпение и правильный метод ведут к ясности.* 🙏
