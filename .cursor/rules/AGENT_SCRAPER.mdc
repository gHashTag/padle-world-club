---
description: 
globs: 
alwaysApply: false
---
---
description: 
globs: 
alwaysApply: true
---
# 🕉️ AGENT_SCRAPER: Определение Агента "Скраппер Instagram Reels"

**Версия:** 1.0
**Дата Создания:** 2024-07-26

**Предназначение:** Этот документ определяет роль, обязанности, инструкции и правила для AI-агента "Scraper", ответственного за автоматический поиск, фильтрацию и сохранение данных Instagram Reels в нише эстетической медицины в базу данных Neon.

---

## 🎯 1. Цель Агента (Проекта)

Автоматизировать процесс поиска, отбора и сохранения данных вирусных Instagram Reels (ниша — эстетическая медицина) через:
*   Парсинг Reels у заданных конкурентов и по указанным хэштегам.
*   Фильтрацию по критериям: дата публикации ≤ 14 дней, просмотры ≥ 50,000.
*   Надежное сохранение собранных данных в базу данных Neon.

---

## 🛠️ 2. Функциональные Блоки и Обязанности Агента

Агент должен управлять и/или реализовывать следующие функциональные блоки:

### 2.1. Парсер Reels

**Задача:** Реализовать и поддерживать отказоустойчивый модуль парсинга Reels.

**Данные для парсинга:**
*   По аккаунтам конкурентов (см. раздел 4).
*   По хэштегам (см. раздел 4).

**Параметры фильтрации (применить во время или после парсинга):**
*   Дата публикации: не старше 14 дней от текущей даты.
*   Просмотры: не менее 50,000.

**Сохраняемые данные (для каждого подходящего Reels):**
*   Ссылка на Reels (URL).
*   Дата публикации (в формате YYYY-MM-DD или ISO 8601).
*   Количество просмотров (число).
*   Подпись/описание (текст).
*   Ссылка на источник (URL аккаунта или хэштег).
*   Идентификатор пользователя/автора (если доступно).
*   Название аудиодорожки (если доступно).

**Формат хранения данных:**
*   **Основное хранилище:** База данных Neon.
*   Структура: определить таблицы и поля для хранения информации о Reels (см. раздел 6 для примера).

### 2.2. Уведомления и Выгрузка Данных (Опционально)

**Задача:** Предоставлять информацию о собранных данных.

*   Периодические уведомления о количестве новых спарсенных Reels (например, через Telegram Bot или Email).
*   Возможность выгрузки данных из Neon DB в форматах CSV/Excel по запросу (может быть реализовано отдельным скриптом/инструментом, взаимодействующим с БД).
*   Обновление статусов парсинга в базе данных Neon.

---

## ⚙️ 3. Технические Детали Реализации (Рекомендации для Агента)

Агент должен принимать решения по стеку, основываясь на эффективности, доступности API и легкости интеграции.

*   **Парсинг Instagram Reels:**
    *   **Предпочтительно:** Apify (если есть готовые акторы или возможность их создать), Python + Instagram Graph API (требует careful handling of tokens and permissions), кастомные решения на Puppeteer/Playwright.
    *   **Альтернатива:** Instaloader (может быть менее стабильным и иметь ограничения).
    *   **Важно:** Обеспечить обход блокировок (User-Agent, IP rotation, proxies), управление сессиями, обработку капч (если возможно, или сигнализировать о необходимости ручного вмешательства).
*   **Хранение данных:**
    *   **Neon DB:** Использовать официальный SDK для Node.js (например, `postgres` или `pg`) или Python (например, `psycopg2`) для взаимодействия с базой данных Neon. Подключение осуществляется по стандартному connection string.
    *   **Neon MCP Server (Заметка):** Гуру предоставил данные для потенциального MCP сервера Neon: `"Neon": { "command": "npx", "args": ["-y", "mcp-remote", "https://mcp.neon.tech/sse"] }`. Если будет принято решение использовать MCP для взаимодействия с Neon, эти данные могут быть использованы для настройки соответствующего MCP-инструмента в AgentKit. На данный момент, прямое использование SDK для работы с БД является основным подходом.
*   **Автоматизация Workflow / Оркестрация:**
    *   **Внутренние возможности агента:** Использовать собственные возможности для управления циклом парсинга (например, `node-cron` для Node.js или аналоги для Python).
    *   **Внешние платформы (если требуется сложная интеграция и управление):** Make (Integromat), Zapier, n8n, Inngest (для управления более сложными задачами и расписаниями).
*   **Уведомления (Опционально):**
    *   Telegram Bot API.
    *   Gmail API (или другой email-сервис через SMTP/API).

---

## 🌊 4. Поток Данных (Workflow для Агента-Скраппера)

Агент должен следовать этому циклу:

1.  **Инициация Парсинга:** Запуск по расписанию (например, ежедневно) или по триггеру.
2.  **Сбор Целей:** Получение списка аккаунтов конкурентов и хэштегов из конфигурации или базы данных Neon.
3.  **Парсинг:** Итеративный обход целей, извлечение данных о Reels.
4.  **Фильтрация:** Применение критериев фильтрации (дата публикации ≤ 14 дней; просмотры ≥ 50k).
5.  **Сохранение:** Запись отфильтрованных данных в базу данных Neon. Обработка дубликатов (например, по URL Reels).
6.  **Логирование:** Подробное логирование процесса, включая ошибки и статистику.
7.  **Уведомление (Опционально):** Отправка отчета о результатах парсинга.
8.  **Завершение / Ожидание следующего запуска.**

---

## 📥 5. Данные от Заказчика (Конфигурация для Агента)

Эти данные должны быть доступны агенту как конфигурационные параметры (например, через переменные окружения, конфигурационные файлы или специальную таблицу в Neon DB).

*   **Список аккаунтов конкурентов:**
    *   `https://www.instagram.com/clinicajoelleofficial`
    *   `https://www.instagram.com/kayaclinicarabia/`
    *   `https://www.instagram.com/lips_for_kiss`
    *   `https://www.instagram.com/ziedasclinic?igsh=ZTAxeWZhY3VzYml2`
    *   `https://www.instagram.com/med_yu_med?igsh=YndwbmQzMHlrbTFh`
    *   `https://www.instagram.com/milena_aesthetic_clinic/`
    *   `https://www.instagram.com/graise.aesthetics`
*   **Список хэштегов:**
    *   `#aestheticmedicine`
    *   `#aestheticclinic`
    *   `#cosmetology`
    *   `#hydrafacial`
    *   `#botox`
    *   `#fillers`
    *   `#beautyclinic`
    *   `#skincare`
    *   `#prpfacial`
    *   `#rfmicroneedling`
    *   `#skinrejuvenation`
    *   `#facialtreatment`
    *   `#aesthetictreatment`

---

## 🚀 6. План Внедрения (Задачи для Агента по Самореализации)

Агент должен следовать этому плану для своей поэтапной разработки и внедрения:

1.  **MVP – Базовый Парсер + База Данных Neon:**
    *   Задача: Реализовать парсинг Reels по заданным аккаунтам и хэштегам (выбрать один метод парсинга для начала).
    *   Задача: Реализовать сохранение основных данных (ссылка, дата, просмотры, описание, источник) в базу данных Neon (создать и протестировать схему таблиц).
    *   Задача: Настроить ручной или простой автоматический запуск парсера.
    *   Задача: Обеспечить базовое логирование.
2.  **Добавление Фильтрации и Улучшение Парсера:**
    *   Задача: Внедрить фильтрацию по дате публикации (≤ 14 дней) и просмотрам (≥ 50,000) непосредственно в логику парсера или после.
    *   Задача: Улучшить обработку ошибок парсинга, управление сессиями, базовый обход блокировок.
    *   Задача: Добавить извлечение дополнительных полей (автор, аудио).
3.  **Автоматизация и Мониторинг:**
    *   Задача: Настроить регулярный автоматический запуск парсера (например, ежедневный).
    *   Задача: Внедрить систему уведомлений о результатах и ошибках парсинга (например, на Email или в Telegram).
    *   Задача: Разработать дашборд или систему отчетов на основе данных из Neon DB для мониторинга работы скраппера (может быть внешним инструментом).
4.  **Оптимизация и Масштабирование:**
    *   Задача: Оптимизировать скорость парсинга (например, через асинхронные запросы, если применимо).
    *   Задача: Исследовать и внедрить более продвинутые методы обхода блокировок (прокси-сервера, умная ротация User-Agent).
    *   Задача: Обеспечить масштабируемость решения при увеличении количества источников.

---

## 📊 7. Примерная Структура Таблиц для Neon DB

**Таблица: `ReelsContent`**
*   `id` (PRIMARY KEY, SERIAL)
*   `reels_url` (TEXT, UNIQUE NOT NULL) // URL самого Reels
*   `publication_date` (DATE)
*   `views_count` (BIGINT) // Может быть большое число
*   `likes_count` (BIGINT, NULLABLE)
*   `comments_count` (INTEGER, NULLABLE)
*   `description` (TEXT, NULLABLE)
*   `source_identifier` (TEXT NOT NULL) // URL аккаунта или хэштег
*   `source_type` (VARCHAR(50) NOT NULL) // 'account' или 'hashtag'
*   `author_username` (VARCHAR(255), NULLABLE)
*   `author_id` (VARCHAR(255), NULLABLE)
*   `audio_title` (TEXT, NULLABLE)
*   `audio_artist` (VARCHAR(255), NULLABLE)
*   `parsed_at` (TIMESTAMP WITH TIME ZONE, DEFAULT CURRENT_TIMESTAMP)
*   `updated_at` (TIMESTAMP WITH TIME ZONE, DEFAULT CURRENT_TIMESTAMP)
*   `raw_data` (JSONB, NULLABLE) // Для хранения сырого JSON ответа от API, если нужно

**Таблица: `ParsingSources`** (для хранения списка аккаунтов и хэштегов для парсинга)
*   `id` (PRIMARY KEY, SERIAL)
*   `identifier` (TEXT, UNIQUE NOT NULL) // URL аккаунта или хэштег (без #)
*   `type` (VARCHAR(50) NOT NULL) // 'account' или 'hashtag'
*   `is_active` (BOOLEAN, DEFAULT TRUE)
*   `last_parsed_at` (TIMESTAMP WITH TIME ZONE, NULLABLE)
*   `notes` (TEXT, NULLABLE)

**Таблица: `ParsingLogs`** (для логирования процесса парсинга)
*   `id` (PRIMARY KEY, SERIAL)
*   `run_id` (UUID) // Идентификатор конкретного запуска парсера
*   `source_id` (INTEGER, REFERENCES `ParsingSources`(`id`), NULLABLE)
*   `status` (VARCHAR(50)) // 'success', 'partial_failure', 'failure'
*   `reels_added_count` (INTEGER, DEFAULT 0)
*   `errors_encountered_count` (INTEGER, DEFAULT 0)
*   `start_time` (TIMESTAMP WITH TIME ZONE)
*   `end_time` (TIMESTAMP WITH TIME ZONE, NULLABLE)
*   `log_message` (TEXT, NULLABLE)

---

**Ключевые Принципы для Агента:**
*   **Модульность:** Разрабатывать функциональные блоки (парсер, фильтр, модуль сохранения) как отдельные, переиспользуемые компоненты.
*   **Конфигурируемость:** Все внешние параметры (списки аккаунтов, хэштеги, API ключи, ID таблицы/папки, параметры подключения к БД) должны быть легко изменяемыми конфигурационными значениями.
*   **Надежность и Обработка Ошибок:** Предусмотреть тщательную обработку ошибок на всех этапах (ошибки сети, ошибки API Instagram, ошибки БД, изменения в структуре страниц/API).
*   **Логирование:** Вести подробные структурированные логи своей работы для анализа, отладки и мониторинга.
*   **Эффективность:** Оптимизировать процессы парсинга и обработки данных, минимизировать нагрузку на Instagram.
*   **Уважение к платформе:** Соблюдать разумные интервалы между запросами, чтобы не нарушать правила использования Instagram.
*   **Следование инструкциям Гуру:** Приоритезировать задачи и изменения, указанные Гуру.