---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Быстрый Запрос Документации через Context7

Этот документ содержит информацию для быстрого получения актуальной документации по часто используемым библиотекам с использованием инструментов Context7.

**Цель:** Избежать повторного поиска ID библиотек.

---

## 📚 Bun

*   **Библиотека:** Bun
*   **Найденный Context7 ID:** `/oven-sh/bun`
*   **Пример Темы:** `testing`

**🕉️ Ритуал Запроса Документации (Пример для темы testing):**

1.  **Уточнение ID (если необходимо):**
    ```tool_code
    print(default_api.mcp_context7_resolve-library-id(libraryName="Bun"))
    ```
2.  **Получение Документации:**
    ```tool_code
    print(default_api.mcp_context7_get-library-docs(context7CompatibleLibraryID="/oven-sh/bun", topic="testing"))
    ```
    *(Убери `topic="testing"` для получения общей документации)*

---

## 🤖 AgentKit (Inngest)

*   **Библиотека:** AgentKit
*   **Найденный Context7 ID:** `/inngest/agent-kit`

**🕉️ Ритуал Запроса Документации:**

1.  **Уточнение ID (если необходимо):**
    ```tool_code
    print(default_api.mcp_context7_resolve-library-id(libraryName="AgentKit"))
    ```
2.  **Получение Документации:**
    ```tool_code
    print(default_api.mcp_context7_get-library-docs(context7CompatibleLibraryID="/inngest/agent-kit"))
    ```
    *(Добавь `topic="<нужная_тема>"` для фокусировки)*

---

## 🐘 Supabase

*   **Библиотека:** Supabase
*   **Найденный Context7 ID:** `/supabase/supabase`

**🕉️ Ритуал Запроса Документации:**

1.  **Уточнение ID (если необходимо):**
    ```tool_code
    print(default_api.mcp_context7_resolve-library-id(libraryName="Supabase"))
    ```
2.  **Получение Документации:**
    ```tool_code
    print(default_api.mcp_context7_get-library-docs(context7CompatibleLibraryID="/supabase/supabase"))
    ```
    *(Добавь `topic="<нужная_тема>"` для фокусировки)*

---

*Ом Шанти. Да пребудет знание под рукой.* 🙏
